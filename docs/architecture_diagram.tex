% =============================================================================
% Publication-Quality Architecture Diagram: Generative VQA with MOE Fusion
% Compatible with NeurIPS / CVPR / KES-style papers
% Usage:  \input{architecture_diagram.tex}   (inside a figure environment)
%    or:  pdflatex architecture_diagram.tex   (standalone compilation)
% =============================================================================
\documentclass[border=10pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{tikz}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}

\usetikzlibrary{
    arrows.meta,
    positioning,
    shapes.geometric,
    calc,
    fit,
    backgrounds,
    decorations.pathreplacing,
    patterns
}

% ── Colour palette (muted, print-safe) ──────────────────────────────────────
\definecolor{visBlue}{HTML}{2E6DAD}
\definecolor{visBg}{HTML}{D6E6F5}
\definecolor{txtGreen}{HTML}{237A45}
\definecolor{txtBg}{HTML}{D4EDDE}
\definecolor{moeRed}{HTML}{C0392B}
\definecolor{moeBg}{HTML}{FDECEC}
\definecolor{moeBorder}{HTML}{D45A5A}
\definecolor{routerOrange}{HTML}{D98B2B}
\definecolor{routerBg}{HTML}{FDF3E2}
\definecolor{expPurple}{HTML}{7B5EA7}
\definecolor{expBg}{HTML}{EDE6F6}
\definecolor{decPurple}{HTML}{6A4C93}
\definecolor{decBg}{HTML}{E8E0F0}
\definecolor{outGray}{HTML}{4A4A4A}
\definecolor{outBg}{HTML}{F0F0F0}
\definecolor{arrGray}{HTML}{555555}
\definecolor{dimGray}{HTML}{888888}
\definecolor{lossRed}{HTML}{B03A2E}
\definecolor{gradGreen}{HTML}{2E7D32}
\definecolor{sparseBlue}{HTML}{1565C0}
\definecolor{frozenGray}{HTML}{95A5A6}
\definecolor{inputBlue}{HTML}{4A90D9}
\definecolor{inputBg}{HTML}{EBF2FA}

% ── Reusable styles ─────────────────────────────────────────────────────────
\tikzset{
  blk/.style={draw,rounded corners=3pt,minimum height=0.85cm,
              font=\small\sffamily,align=center,thick,inner sep=5pt},
  inblk/.style ={blk,fill=inputBg,draw=inputBlue,minimum width=3cm},
  vblk/.style  ={blk,fill=visBg,draw=visBlue,minimum width=3.4cm},
  tblk/.style  ={blk,fill=txtBg,draw=txtGreen,minimum width=3.4cm},
  rblk/.style  ={blk,fill=routerBg,draw=routerOrange,minimum width=4.5cm},
  eblk/.style  ={blk,fill=expBg,draw=expPurple,
                  minimum width=2.1cm,minimum height=1.05cm,
                  font=\footnotesize\sffamily},
  dblk/.style  ={blk,fill=decBg,draw=decPurple,minimum width=5.5cm},
  oblk/.style  ={blk,fill=outBg,draw=outGray,minimum width=3.5cm},
  op/.style     ={draw,rounded corners=2pt,minimum height=0.50cm,
                  font=\scriptsize\sffamily,align=center,thin,
                  inner sep=3pt,fill=white},
  dim/.style    ={font=\scriptsize\ttfamily,text=dimGray},
  frozen/.style ={font=\tiny\sffamily\itshape,text=frozenGray},
  stg/.style    ={font=\footnotesize\sffamily\bfseries,text=dimGray},
  arr/.style    ={-{Stealth[length=5pt,width=4pt]},thick,arrGray},
  sparr/.style  ={-{Stealth[length=5pt,width=4pt]},thick,sparseBlue,dashed},
  auxarr/.style ={-{Stealth[length=4pt,width=3pt]},semithick,lossRed,
                  densely dashed},
  gradarr/.style={-{Stealth[length=4pt,width=3pt]},semithick,gradGreen,
                  densely dotted},
}

\begin{document}
\begin{tikzpicture}[node distance=0.55cm and 1cm]

% ═══════════════════════════════════════════════════════════════════════════════
%  STAGE 1 — INPUTS
% ═══════════════════════════════════════════════════════════════════════════════
\node[stg] at (-6.8,0) {\rotatebox{90}{INPUT}};

\node[inblk] (imgIn) at (-2.8,0) {\textbf{Image}~$I$};
\node[dim,below=1pt of imgIn] {$224{\times}224{\times}3$};

\node[inblk] (qIn) at (2.8,0) {\textbf{Question}~$Q$};
\node[dim,below=1pt of qIn] {$L$ tokens (Vietnamese)};

% ═══════════════════════════════════════════════════════════════════════════════
%  STAGE 2/3 — ENCODERS  (Visual left, Text right)
% ═══════════════════════════════════════════════════════════════════════════════
\node[stg] at (-6.8,-2.5) {\rotatebox{90}{ENCODERS}};

% --- Visual column ---
\node[vblk] (patchEmb) at (-2.8,-1.3)
  {Patch Embedding\\[-2pt]{\scriptsize $32{\times}32$ patches + \texttt{[CLS]}}};
\node[dim,right=2pt of patchEmb] {$50{\times}d_v$};

\node[vblk] (vitEnc) at (-2.8,-2.5)
  {\textbf{CLIP ViT-B/32}\\[-2pt]{\scriptsize Transformer Encoder}};
\node[frozen,right=2pt of vitEnc] {\textit{frozen}};

\node[op,draw=visBlue,fill=visBg!40] (visProj) at (-2.8,-3.6)
  {Linear~$W_v$};
\node[dim,right=2pt of visProj] {$50{\times}D$};

% --- Text column ---
\node[tblk] (tokEmb) at (2.8,-1.3)
  {Token Embedding\\[-2pt]{\scriptsize + Position Encoding}};
\node[dim,left=2pt of tokEmb] {$L{\times}d_t$};

\node[tblk] (phoBERT) at (2.8,-2.5)
  {\textbf{PhoBERT-base}\\[-2pt]{\scriptsize Transformer Encoder}};
\node[frozen,left=2pt of phoBERT] {\textit{frozen}};

\node[op,draw=txtGreen,fill=txtBg!40] (txtProj) at (2.8,-3.6)
  {Linear~$W_t$};
\node[dim,left=2pt of txtProj] {$L{\times}D$};

% Encoder arrows
\draw[arr] (imgIn)    -- (patchEmb);
\draw[arr] (patchEmb) -- (vitEnc);
\draw[arr] (vitEnc)   -- (visProj);
\draw[arr] (qIn)      -- (tokEmb);
\draw[arr] (tokEmb)   -- (phoBERT);
\draw[arr] (phoBERT)  -- (txtProj);

% ═══════════════════════════════════════════════════════════════════════════════
%  CONCATENATION
% ═══════════════════════════════════════════════════════════════════════════════
\node[op,minimum width=4.8cm,draw=arrGray] (concat) at (0,-4.8)
  {Concatenation~$[\hat{V};\;\hat{T}]$};
\node[dim,below=1pt of concat] (concatDim) {$(N_v{+}L){\times}D$};

\draw[arr] (visProj.south) -- ++(0,-0.35) -| ([xshift=-1cm]concat.north);
\draw[arr] (txtProj.south) -- ++(0,-0.35) -| ([xshift= 1cm]concat.north);

% ═══════════════════════════════════════════════════════════════════════════════
%  STAGE 4 — CROSS-MODAL FUSION WITH MOE  (core contribution, highlighted)
% ═══════════════════════════════════════════════════════════════════════════════
\node[stg] at (-6.8,-8.0) {\rotatebox{90}{FUSION~(MOE)}};

% (a) Pre-norm Transformer attention
\node[blk,fill=moeBg!60,draw=moeRed!70,minimum width=5.5cm]
  (preAttn) at (0,-6.0)
  {\textbf{Pre-Norm Transformer}~${\times}\,L_f{=}2$\\[-2pt]
   {\scriptsize MHSA~($H{=}8$) + FFN~($d_{\mathrm{ff}}{=}2048$) + LN}};
\node[dim,right=2pt of preAttn] {$(N_v{+}L){\times}D$};

% (b) Noisy Top-k Router
\node[rblk] (router) at (0,-7.4)
  {\textbf{Noisy Top-2 Router}\\[-2pt]
   {\scriptsize $\tilde{\ell}=W_r^\top x
     +\mathrm{Softplus}(W_n^\top x)\!\cdot\!\epsilon$\,,\;
     $\epsilon{\sim}\mathcal{N}(0,\sigma^2 I)$}\\[-2pt]
   {\scriptsize Softmax~$\rightarrow$~Top-2 selection per token}};
\node[dim,right=2pt of router,text=sparseBlue]
  {\textit{sparse:~$k{=}2$ of $K{=}4$}};

% (c) Specialised Experts — placed at fixed x so they centre around x=0
\node[eblk] (eVis)  at (-3.75,-9.1)
  {$E_{\mathrm{vis}}$\\[-1pt]\textbf{Vision}\\[-2pt]
   {\tiny Spatial MHSA}\\[-2pt]{\tiny + FFN}};
\node[eblk] (eTxt)  at (-1.25,-9.1)
  {$E_{\mathrm{txt}}$\\[-1pt]\textbf{Text}\\[-2pt]
   {\tiny Context MHSA}\\[-2pt]{\tiny + Masked FFN}};
\node[eblk] (eMM)   at ( 1.25,-9.1)
  {$E_{\mathrm{mm}}$\\[-1pt]\textbf{Multimodal}\\[-2pt]
   {\tiny Cross-Attn}\\[-2pt]{\tiny + Gate~$\gamma$}};
\node[eblk] (eSpec) at ( 3.75,-9.1)
  {$E_{\mathrm{spec}}$\\[-1pt]\textbf{Specialized}\\[-2pt]
   {\tiny Seg\,/\,Det\,/}\\[-2pt]{\tiny OCR\,/\,Scene}};

% (d) Weighted aggregation
\node[op,minimum width=6cm,draw=moeRed!70,fill=moeBg!50]
  (agg) at (0,-10.7)
  {Weighted Aggregation~+~LayerNorm};
\node[dim,below=1pt of agg] (aggDim)
  {$y=\mathrm{LN}\!\bigl(\sum_{i\in\mathrm{Top\text{-}2}}
    g_i\cdot E_i(x)\bigr)$\qquad
   $(N_v{+}L){\times}D$};

% Load-balancing loss (auxiliary side branch)
\node[blk,fill=white,draw=lossRed,minimum width=2.4cm,
      font=\scriptsize\sffamily] (lbLoss) at (6.2,-7.4)
  {$\mathcal{L}_{\mathrm{lb}}
     =K\!\displaystyle\sum f_i\bar{p}_i$\\[-2pt]
   {\tiny $\lambda_{\mathrm{lb}}{=}0.01$}};
\node[dim,above=1pt of lbLoss,text=lossRed]
  {\textit{Load-Balance Loss}};

% ── MOE highlighted background ──────────────────────────────────────────────
\begin{scope}[on background layer]
  \node[draw=moeBorder,fill=moeBg!20,rounded corners=6pt,
        very thick,inner sep=10pt,
        fit=(preAttn)(router)(eVis)(eSpec)(agg)(aggDim)(lbLoss)]
    (moeFrame) {};
  \node[font=\small\sffamily\bfseries,text=moeRed,
        anchor=north west]
    at ([xshift=4pt,yshift=-2pt]moeFrame.north west)
    {Cross-Modal Fusion with Mixture-of-Experts};
\end{scope}

% ── Arrows inside MOE ───────────────────────────────────────────────────────
\draw[arr] (concat) -- (preAttn);
\draw[arr] (preAttn) -- (router);

% Router → experts  (sparse dashed blue)
\draw[sparr] (router.south) -- ++(0,-0.30) -| (eVis.north);
\draw[sparr] (router.south) -- ++(0,-0.30) -| (eTxt.north);
\draw[sparr] (router.south) -- ++(0,-0.30) -| (eMM.north);
\draw[sparr] (router.south) -- ++(0,-0.30) -| (eSpec.north);

% Experts → aggregation
\draw[arr] (eVis.south)  -- ++(0,-0.30) -| ([xshift=-1.8cm]agg.north);
\draw[arr] (eTxt.south)  -- ++(0,-0.35) -| ([xshift=-0.6cm]agg.north);
\draw[arr] (eMM.south)   -- ++(0,-0.35) -| ([xshift= 0.6cm]agg.north);
\draw[arr] (eSpec.south) -- ++(0,-0.30) -| ([xshift= 1.8cm]agg.north);

% Router → load-balance loss
\draw[auxarr] (router.east) -- (lbLoss.west);

% ═══════════════════════════════════════════════════════════════════════════════
%  STAGE 5 — GENERATIVE DECODER
% ═══════════════════════════════════════════════════════════════════════════════
\node[stg] at (-6.8,-12.2) {\rotatebox{90}{DECODER}};

\node[dblk] (decoder) at (0,-12.2)
  {\textbf{Autoregressive Transformer Decoder}~${\times}\,L_d{=}6$\\[-2pt]
   {\scriptsize Causal Self-Attn~$\to$~Cross-Attn~$\to$~FFN}\\[-2pt]
   {\scriptsize $H_d{=}8$ heads,\; $d_{\mathrm{ff}}{=}2048$,\; Pre-Norm}};
\node[dim,right=2pt of decoder]
  {\parbox{2.2cm}{\centering\scriptsize encoder memory\\
   $(N_v{+}L){\times}D$}};

% Teacher-forcing input (left)
\node[op,draw=decPurple,fill=decBg!40,minimum width=1.8cm]
  (tfEmb) at (-5.0,-12.2)
  {\parbox{1.6cm}{\centering Embed.~$W_e$\\{\tiny + Pos Enc}\\
   {\tiny (teacher forcing)}}};
\node[dim,above=1pt of tfEmb] {$a_{<t}$};

\draw[arr] (agg) -- (decoder);
\draw[arr] (tfEmb) -- (decoder);

% ═══════════════════════════════════════════════════════════════════════════════
%  STAGE 6 — OUTPUT
% ═══════════════════════════════════════════════════════════════════════════════
\node[stg] at (-6.8,-14.5) {\rotatebox{90}{OUTPUT}};

\node[op,draw=outGray,minimum width=4.2cm] (outProj) at (0,-13.5)
  {Output Projection~$W_e^\top$\;\;{\scriptsize (weight-tied)}};
\node[dim,right=2pt of outProj] {$T{\times}|\mathcal{V}|$};

\node[op,draw=outGray,minimum width=2.5cm] (softmax) at (0,-14.2)
  {Softmax};

\node[oblk] (answer) at (0,-15.1)
  {\textbf{Generated Answer}\\[-2pt]
   {\scriptsize Open-ended Vietnamese text}};

\draw[arr] (decoder) -- (outProj);
\draw[arr] (outProj) -- (softmax);
\draw[arr] (softmax) -- (answer);

% ═══════════════════════════════════════════════════════════════════════════════
%  TRAINING LOSS  (combined CE + LB)
% ═══════════════════════════════════════════════════════════════════════════════
\node[blk,fill=white,draw=lossRed,minimum width=3.2cm,
      font=\scriptsize\sffamily] (totalLoss) at (6.2,-13.0)
  {\textbf{Training Loss}\\[-2pt]
   $\mathcal{L}=\mathcal{L}_{\mathrm{ce}}
     +\lambda\,\mathcal{L}_{\mathrm{lb}}$\\[-2pt]
   {\tiny label smoothing~$\epsilon{=}0.1$}};

\draw[auxarr] (softmax.east) -| (totalLoss.south);
\draw[auxarr] (lbLoss.south) |- ([yshift=3pt]totalLoss.north);

% ═══════════════════════════════════════════════════════════════════════════════
%  GRADIENT FLOW ANNOTATION
% ═══════════════════════════════════════════════════════════════════════════════
\draw[gradarr,very thick]
  ([xshift=3.5cm]answer.east) -- ++(0,13.5)
  node[midway,right=3pt,font=\scriptsize\sffamily,
       text=gradGreen,rotate=90,anchor=south]
  {$\nabla$~Gradient flow (backpropagation)};

% Frozen-backbone note
\node[font=\tiny\sffamily,text=frozenGray,align=center,
      fill=white,draw=frozenGray,rounded corners=2pt,inner sep=2pt]
  (frozenNote) at (-5.8,-2.5)
  {\textsf{\textbf{$\times$}}~Frozen backbones:\\no gradient update};

\draw[frozenGray,thin,densely dotted]
  (frozenNote.east) -- ++(0.15,0) |- (vitEnc.west);
\draw[frozenGray,thin,densely dotted]
  (frozenNote.east) -- ++(0.15,0) |- (phoBERT.west);

% ═══════════════════════════════════════════════════════════════════════════════
%  LEGEND  (top-right)
% ═══════════════════════════════════════════════════════════════════════════════
\node[anchor=north west,font=\scriptsize\sffamily,text=dimGray,
      align=left,fill=white,draw=dimGray!50,
      rounded corners=3pt,inner sep=5pt]
  (legend) at (7.8,-0.2)
  {\textbf{Legend}\\[3pt]
   \tikz[baseline=-0.5ex]\draw[arr](0,0)--(0.55,0);
     ~Dense data flow\\[1pt]
   \tikz[baseline=-0.5ex]\draw[sparr](0,0)--(0.55,0);
     ~Sparse dispatch\\[1pt]
   \tikz[baseline=-0.5ex]\draw[auxarr](0,0)--(0.55,0);
     ~Auxiliary loss\\[1pt]
   \tikz[baseline=-0.5ex]\draw[gradarr](0,0)--(0.55,0);
     ~Gradient flow\\[4pt]
   \tikz\fill[visBg](0,0) rectangle (0.35,0.22);
     ~Visual pathway\\[1pt]
   \tikz\fill[txtBg](0,0) rectangle (0.35,0.22);
     ~Text pathway\\[1pt]
   \tikz\fill[moeBg](0,0) rectangle (0.35,0.22);
     ~MOE fusion (core)\\[1pt]
   \tikz\fill[decBg](0,0) rectangle (0.35,0.22);
     ~Decoder};

% ═══════════════════════════════════════════════════════════════════════════════
%  COMPUTATIONAL PROPERTIES  (right)
% ═══════════════════════════════════════════════════════════════════════════════
\node[anchor=north west,font=\scriptsize\sffamily,text=dimGray,
      align=left,fill=white,draw=dimGray!50,
      rounded corners=3pt,inner sep=5pt]
  (props) at (7.8,-4.2)
  {\textbf{Computational Properties}\\[3pt]
   \textcolor{sparseBlue}{\rule{7pt}{2pt}}~%
     Sparse activation: Top-2 of $K{=}4$\\[1pt]
   \textcolor{sparseBlue}{\rule{7pt}{2pt}}~%
     Sparsity ratio: $k/K=50\%$ per token\\[1pt]
   Capacity factor: $\alpha=1.25$\\[1pt]
   $D=d_v=d_t=768$\\[1pt]
   $|\mathcal{V}|=64{,}000$ (PhoBERT vocab)\\[1pt]
   Weight tying: $W_e$ shared in\\
   \quad decoder embed.\ \& output proj.\\[3pt]
   \textcolor{gradGreen}{\rule[1pt]{7pt}{1.5pt}}~%
     Gradients flow through\\
   \quad fusion, MOE, decoder\\[1pt]
   \textcolor{frozenGray}{\textbf{$\times$}}~%
     No gradient to frozen encoders\\[3pt]
   \textit{Dense fusion complexity:}\;$\mathcal{O}(S^2 D)$\\
   \textit{Sparse MOE complexity:}\;$\mathcal{O}(S\!\cdot\!k\!\cdot\!D)$};

\end{tikzpicture}
\end{document}
